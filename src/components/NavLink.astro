---
const { href, title, active } = Astro.props;
const path = new URL(Astro.request.url).pathname;
---

<a role="button" href={href} title={title} class={path.startsWith(href) ? `${active}`: ''}>
  <slot/>
</a>